@model IEnumerable<TvShop.Models.Order>

<h2>All Orders</h2>

<table class="table">
    <thead>
        <tr>
            <th>Order ID</th>
            <th>User ID</th>
            <th>Total Amount</th>
            <th>Order Date</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var order in Model)
        {
            <tr>
                <td>@order.Id</td>
                <td>@order.UserId</td>
                <td>₹@order.TotalAmount</td>
                <td>@order.OrderDate</td>
                <td>
    <a asp-action="OrderDetails" asp-route-id="@order.Id">View Details</a>
    <a asp-action="UpdateStatus" asp-route-id="@order.Id">Mark as Shipped</a>
</td>
            </tr>
        }
    </tbody>
</table>